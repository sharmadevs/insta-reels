import LightTheme from './LightTheme';
import DarkTheme from './DarkTheme';
import APPSTRING from './String';
export const DEFAULT_THEME = APPSTRING.LIGHT;

export const Theme = {
  light: LightTheme,
  dark: DarkTheme,
};
